getwd()
#Load data
sites = read.csv("C:/Users/jacog/Desktop/github-notebooks/JMG_Puritz_Lab_Notebook/data/sample_sites.csv")
#Look at the data
dim(sites)
head(sites)
attach(sites)
#Input google key
register_google(key="AIzaSyC1Lvx1A1kULrf0BIwlyiytK27uDzQ0YMs")
library(ggmap)
#Input google key
register_google(key="AIzaSyC1Lvx1A1kULrf0BIwlyiytK27uDzQ0YMs")
View(sites)
View(sites)
# Place map points
p <- ggmap(get_googlemap(center = c(lon = 41.650043, lat = -71.348952),
zoom = 11, scale = 2,
maptype ='terrain',
color = 'color'))
p + geom_point(aes(x = lon, y = lat,  colour = Initial.Type.Group), size = 0.5) +
theme(legend.position="bottom")
p + geom_point(aes(x = lon, y = lat,  colour = location), size = 0.5) +
theme(legend.position="bottom")
#Load data
sites = read.csv("C:/Users/jacog/Desktop/github-notebooks/JMG_Puritz_Lab_Notebook/data/sample_sites.csv", header = TRUE)
#Look at the data
dim(sites)
head(sites)
attach(sites)
# Place map points
p <- ggmap(get_googlemap(center = c(lon = 41.650043, lat = -71.348952),
zoom = 11, scale = 2,
maptype ='terrain',
color = 'color'))
p + geom_point(aes(x = lon, y = lat,  colour = location), size = 0.5) +
theme(legend.position="bottom")
p + geom_point(aes(x = lon, y = lat), size = 0.5) +
theme(legend.position="bottom")
samp_mean<-sapply(sites[,c("lon","lat")],mean)
pmap <- get_map(location = samp_mean, maptype = "terrain", source = "google",zoom=3)
ben_map<-ggmap(pmap) + geom_count(data = sites, mapping = aes(x = lon, y = lat), color="red")
ben_map
pmap <- get_map(location = samp_mean, maptype = "terrain", source = "google",zoom=11)
ben_map<-ggmap(pmap) + geom_count(data = sites, mapping = aes(x = lon, y = lat), color="red")
ben_map
pmap <- get_map(location = samp_mean, maptype = "terrain", source = "google",zoom=10)
ben_map<-ggmap(pmap) + geom_count(data = sites, mapping = aes(x = lon, y = lat), color="red")
ben_map
ben_map
